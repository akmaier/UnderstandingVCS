# External dependencies (cloned/downloaded locally, see README.md "External Dependencies")
papers/
dqn/
xitari/

# OS / editor cruft
.DS_Store
**/.DS_Store

# Python (jaxtari)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Julia (jutari)
Manifest.toml
*.jl.cov
*.jl.mem
deps/build.log

# Generated golden traces and ROMs (see PORTING_PLAN.md §4.2)
tests/golden/*.jsonl
tests/golden/roms/

# PXC1 — built C++ binary (built locally from tools/trace_dump.cpp)
tools/trace_dump
tools/*.o

# Diagnostic per-bus-op CSV traces — large (100s of MB), regenerated on demand
# by `tools/cpu_tia_cycle_trace.jl` etc. Not part of the source tree.
tools/fixtures/cycle_traces/

# Per-worktree agent state (claude code agent harness)
.claude/
tools/fixtures/cache/

# Divergence-video frame dumps + mp4 (regenerable via dump_divergence_frames.jl + make_divergence_video.py)
tools/relaxation_study/video_out/
