# Sprint 0 — Backlog Construction

## Goal
Build the full P2 backlog from `SPEC.md`, and validate the agentic-SCRUM mechanism
(parallel workers, disjoint file-scopes, rebase-before-push to `main`).

## Planning
3 developer agents (Opus 4.8), disjoint epic groups (no shared files):
- agent-1 → E0, E1, E2, E3 · agent-2 → E4, E5 · agent-3 → E6, E7, E8, E9
Run as one Workflow (`wf_d90a729a-658`), each agent in its own worktree.

## Review — outcome
- **69 backlog items** created and pushed to `main` in 3 clean commits
  (`0af672e` E0–E3, `d16129a` E4–E5, `1194de3` E6–E9). **No file collisions, no
  rebase retries** — the disjoint-file-scope rule held. Mechanism **validated.**
- Counts: E0:5 · E1:3 · E2:3 · E3:10 · E4:14 · E5:11 · E6:3 · E7:6 · E8:10 · E9:4.
  Where: 45 local · 24 cluster.
- `BOARD.md` generated by `build_board.py` (new SM tool; regenerate at each barrier).

## Clash resolutions (SM decisions; agents flagged these correctly)
1. **E0-1 scope narrowed** from the broad glob `tools/xai_study/common/**` to explicit
   files (`__init__, loader, replay, results, seeds, conftest, test_common`) so it is
   disjoint from E0-2 (`record_state.*`) and E0-4 (`game_set.*`). *(applied)*
2. **`tools/cluster/` is owned solely by E0-3.** E4/E5 cluster items must NOT create new
   `*.sbatch` files; they parametrize E0-3's templates. Enforce at each Planning.
3. **`results_index.csv` is SM-merged at Review** (SPEC §R) — must never appear in any
   per-method `file_scope`. Per-method `out/<method>_*` globs are disjoint by prefix: OK.
4. **`paper/sections/09_*` split:** E8-9 owns `09_endmatter.tex`; E9-1 owns
   `09_statements.tex` (agent already did this). SM adds both `\input`s at E8-0/E9.
5. **`paper/main.tex` + `references.bib` are SM-only at Review** (SCRUM §6). E8-0, E9-2,
   E9-4 are SM tasks — consistent.
6. **E0-5 (`backlog/**`)** = this Sprint-0 fan-out itself (each agent wrote only its
   epic's `P2-E*-*.md`; BOARD.md is SM-only). Mark E0-5 `done` at next Planning.

## Planning guidance for the execution sprints
- **Sprint 3 is oversized (32 items).** It is the experiment wave (A1–A8 + all B/C
  methods); **split into sub-waves (3a, 3b, …)** at Planning: ≤3 *active local* workers,
  but a wave may queue **many cluster** items (they submit-and-return; SM collects at
  Review). Group by disjoint phase trees (phaseA / phaseB / phaseC) and by game.
- **Sprint 1 (6 items, local, low-risk):** E0-2 recorder, E0-4 game-set/T3 audit,
  E1-1/2/3 oracle, E2-1 import — plus pull in the pilots E3-0/E4-0/E5-0 if their deps
  (E1) land first. This is the next sprint to launch.
- Verify per-sprint disjointness with `build_board.py` output before each launch.

## Retro
- Workers were clean; the only hiccup was the **SM worktree** desyncing after the
  concurrent pushes (a botched local rebase left HEAD at an old commit). Fix:
  `git reset --hard origin/main` (safe — all SM commits were already pushed). Lesson:
  at Review, the SM should `git reset --hard origin/main` to sync, not `rebase`.

## Definition of Done — Sprint 0 ✅
Backlog built (69 items) · board generated · disjointness verified/flagged · resolutions
recorded · committed & pushed. **Next: Sprint 1 (oracle + pilots), on the loop.**
