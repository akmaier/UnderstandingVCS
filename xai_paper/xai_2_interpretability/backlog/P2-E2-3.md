---
id: P2-E2-3
title: Discover new T3 labels (correlation + sweep)
epic: E2 (T3 acquisition)
status: done
sprint: 6
owner:
where: local
depends_on: [P2-E2-2]
file_scope:
  - tools/xai_study/t3/discover_labels.*
  - tools/xai_study/t3/out/discovered_*
estimate: L
spec_ref: ../SPEC.md#e2--t3-acquisition-sprint-12-local
---

## Goal
Discover new game-concept labels where no public source (OCAtari/AtariARI) exists, via
RAM↔framebuffer correlation plus intervention sweeps, extending T3 coverage toward 40+ of
the 64 games.

## Definition of Done
- [x] `discover_labels.jl` finds new RAM→concept labels by RAM↔framebuffer correlation + intervention sweeps (source-agnostic discovery)
- [x] extended coverage logged to `out/discovered_<game>.json` (+ sibling `.npz`) per SPEC §R, each label intervention-verified (causal-on-the-bit-exact-framebuffer)
- [x] RAN SYNCHRONOUSLY locally (orchestration directive: foreground, no cluster) — whole 5-game sweep is ~14 s on jutari, so no Slurm array needed; cluster path stays available via E0-3 templates for a wider game set
- [x] nothing outside `file_scope` changed; Paper-1 gates untouched/green (no jutari/jaxtari/xitari core touched)
- [x] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [x] `status: done`

## Notes / handoff
experiment_design.md §2 step (3). **Method:** over a recorded jutari trajectory
(`jutari_record`-style replay, deterministic exploration action stream), (A) detect
"identifiable screen regions" = per-non-bg-colour sprite groups (centroid x/y + pixel
size); (B) correlate every RIOT-RAM cell's time series with each region's x/y/size
(|r|≥0.80 = correlational candidate); (C) the DISCRIMINATOR — clamp each of the 128 RAM
cells across an 8-value sweep, re-render ONE bit-exact frame, fit region centroid/size vs
clamp value; a label is verified-causal iff the region moves with the byte (position:
sprite drawn at every sweep point, |Δcentroid|≥4 px, R²≥0.70; extent: graded resize,
|Δsize|≥16 px, R²≥0.70). The linear-fit intercept gives the render offset for free
(auto-offset, like E2-2). Every label is novelty-checked against the public sources by
reusing `import_labels.py`'s AtariARI dict + OCAtari AST harvest (`novel`=true ⇒ no source
names that RAM index).

**Results (committed §R records):** 30 verified-causal discovered labels across 5 games
(pong control + enduro/pitfall/asteroids/beamrider, the locally-available non-core ROMs) —
17 position + 13 extent; **20 genuinely novel** (no AtariARI/OCAtari source). Validation:
pitfall RAM[$E9] causally re-derives a known player-y label at slope≈1.00, R²=0.9995,
corr_r=0.9997. **Headline gap:** 738 correlation-only cells the intervention REJECTS
(present ≠ used; Hewitt & Liang control-task lesson) — recorded in each record's
`correlation_minus_causation`. Self-checks: `discover_labels.jl --selfcheck` (PASS) +
offline `discover_labels_check.py` (PASS — all artifacts internally consistent).

To extend coverage toward 40+/64 at Review: rsync more ROMs locally (or run the E0-3
cluster array) and `discover_labels.jl --games <list>`; the discovery is source-agnostic so
it works on games with no public T3 source.
