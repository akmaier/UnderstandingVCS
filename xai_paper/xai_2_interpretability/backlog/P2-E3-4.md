---
id: P2-E3-4
title: A4 spike-word / pairwise correlations
epic: E3 (Phase A — neuroscience battery)
status: done
sprint: 4
owner:
where: local
depends_on: [P2-E3-0]
file_scope:
  - tools/xai_study/phaseA_kording/A4_correlations.jl
  - tools/xai_study/phaseA_kording/out/A4_*
estimate: M
spec_ref: ../SPEC.md#e3--phase-a-neuroscience-battery-sprints-23-local-pilot-cluster-full
---

## Goal
Compute pairwise and global correlation structure of the state ("spike-word" analysis) and
test whether the weak-pairwise / strong-global pattern is reproduced, comparing against the
true coupling.

## Definition of Done
- [x] `A4_correlations.jl` computes pairwise + global correlation structure of the state on the core set
- [x] scored: weak-pairwise/strong-global reproduced vs true coupling, written to `out/A4_*` per SPEC §R
- [x] runs from a documented command (analysis = local)
- [x] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [x] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [x] `status: done`

## Notes / handoff
experiment_design.md §4 A4. Ground truth T1 (no T3 needed — concept *families* give the
true variable grouping). **JULIA** path (per the Sprint-4 developer contract: experiments
run on jutari locally; builds on `pilot_si.jl` + `jutari_oracle.jl` + `jutari_record.jl` +
`oracle_intervene.jl`). `A4_correlations.jl` (not `.py`) — `out/A4_*` glob unchanged, so the
file-scope is still disjoint from every other Sprint-4 item.

Command:
`XAI_PRIMARY_REPO=/Users/maier/Documents/code/UnderstandingVCS julia
--project=$XAI_PRIMARY_REPO/jutari tools/xai_study/phaseA_kording/A4_correlations.jl`
(`--game G --selftest` for a single-game self-check; `--games a,b` to subset).

**Ran 6/6 core games** (each self-check PASS: bit-exact jutari re-run + oracle-as-method
positive control Spearman F == 1). Per-game F / S / M (correlation vs the exact
cell-cell coupling oracle) + the over-grouping result:

| game | F | S | M | over-group | pop median\|r\| | pop PR/#cells | WPSG | coupled pairs | window |
|---|---|---|---|---|---|---|---|---|---|
| pong | +0.63 | -0.03 | 1.00 | 0.67 | 0.41 | 2.5/18 | yes | 4/28 | in |
| breakout | +0.63 | +0.07 | 1.00 | 1.00 | 0.13 | 5.2/16 | yes | 4/36 | in |
| space_invaders | -0.03 | -0.05 | 0.07 | 0.93 | 0.20 | 3.0/35 | yes | 15/136 | f120 |
| seaquest | +0.42 | +0.01 | 0.05 | 0.63 | 0.66 | 2.2/37 | no\* | 23/253 | f120 |
| ms_pacman | -0.13 | -0.16 | 0.08 | 0.58 | 0.40 | 3.7/46 | yes | 23/171 | f330\*\* |
| qbert | +0.05 | +0.04 | 0.00 | 0.79 | 0.44 | 2.3/77 | yes | 1/190 | in |

Headline (the quantified-Kording calibration baseline): pairwise correlation tracks the
true causal coupling only weakly (F 0.05–0.63, **negative** for SI / ms_pacman) and
**massively over-groups** (M 0.00–0.08 on the busy games; over-grouping ratio 0.58–1.0 —
correlation lumps causally-unrelated cells that merely co-vary). The **weak-pairwise /
strong-global** spike-word pattern (Schneidman 2006 / Tkačik 2014) is reproduced over the
full RAM population: median |corr| is weak (0.13–0.66) while a few global eigen-modes
dominate (participation ratio 2.2–5.2 effective dims out of 16–77 cells; top-3 eigen-share
0.64–0.87). \*seaquest median |corr| 0.66 is genuinely NOT weak-pairwise → WPSG=false
(honest). \*\*ms_pacman / SI / seaquest scored at a later live-play frame (Ms. Pac-Man's
animated intro means candidate cells are static in the 60-frame window) — an honest
descriptive jutari result (bit-exact jutari re-run still asserted), flagged
`scored_in_conformance_window=false` in the §R record, not an xitari-parity claim.

Artifacts (SPEC §R): `tools/xai_study/phaseA_kording/out/A4_<game>.{json,npz}` for all 6
games (JSON = self-describing record incl. F/S/M, clustering + over-grouping, weak-pairwise/
strong-global, oracle coupling; npz = corr_matrix, oracle_coupling[,_heldout], cluster_label,
grouping_scores, global_structure, triad_FSM).
