---
id: P2-E1-2
title: Gradient oracle (content path)
epic: E1 (Ground-truth oracle)
status: todo
sprint: 1
owner:
where: local
depends_on: [P2-E1-1]
file_scope:
  - tools/xai_study/ground_truth/oracle_grad.*
estimate: M
spec_ref: ../SPEC.md#e1--ground-truth-oracle-sprint-1-local-the-foundation
---

## Goal
Build the companion gradient oracle: `∂y/∂u` plus Integrated Gradients through the
differentiable substrate, **content-path only**, documenting the index/position caveat
(zero naive gradient for discrete index outputs; needs the bilinear sampler).

## Definition of Done
- [ ] `oracle_grad.*` produces a content-path gradient map (`∂y/∂u`) and IG for a content output (e.g. a colour/graphics-bit driven pixel) on Pong
- [ ] documents and tests the zero-on-index behavior: discrete index/position outputs have ~0 naive gradient; the bilinear sampler is the workaround
- [ ] runs from a documented command; arrays written as siblings of the E1-1 oracle records
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
experiment_design.md §1 "Differential effect (companion, content-path only)". The gradient
is the oracle's companion ONLY for content outputs; for position/index/event outputs the
intervention oracle (P2-E1-1) is the sole reference and gradient methods are evaluated as
methods-under-test in Phase B (E4), not as oracle.
