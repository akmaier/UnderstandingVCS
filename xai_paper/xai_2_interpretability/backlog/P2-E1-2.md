---
id: P2-E1-2
title: Gradient oracle (content path)
epic: E1 (Ground-truth oracle)
status: done
sprint: 1
owner:
where: local
depends_on: [P2-E1-1]
file_scope:
  - tools/xai_study/ground_truth/oracle_grad.*
estimate: M
spec_ref: ../SPEC.md#e1--ground-truth-oracle-sprint-1-local-the-foundation
---

## Goal
Build the companion gradient oracle: `∂y/∂u` plus Integrated Gradients through the
differentiable substrate, **content-path only**, documenting the index/position caveat
(zero naive gradient for discrete index outputs; needs the bilinear sampler).

## Definition of Done
- [x] `oracle_grad.*` produces a content-path gradient map (`∂y/∂u`) and IG for a content output (e.g. a colour/graphics-bit driven pixel) on Pong
- [x] documents and tests the zero-on-index behavior: discrete index/position outputs have ~0 naive gradient; the bilinear sampler is the workaround
- [x] runs from a documented command; arrays written as siblings of the E1-1 oracle records
- [x] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [x] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [x] `status: done`

## Notes / handoff
experiment_design.md §1 "Differential effect (companion, content-path only)". The gradient
is the oracle's companion ONLY for content outputs; for position/index/event outputs the
intervention oracle (P2-E1-1) is the sole reference and gradient methods are evaluated as
methods-under-test in Phase B (E4), not as oracle.

**Done (jutari).** Built `tools/xai_study/ground_truth/oracle_grad.jl` (Julia/Zygote;
reuses `tools/xai_si_gradient`'s `soft_ram_peek` content read + bilinear `tri` sampler and
`common/jutari_oracle.jl`). Run on Pong (target_frame=120, horizon=30, ig_steps=64):
- (A) CONTENT path — `∂pixel/∂COLUP1 = 1.0` (nonzero ⇒ valid companion); IG attribution
  = 200.0, IG completeness err = 0.0; intervention-oracle finite Δ of the SAME content
  cause (frame-local register poke) = 1.0 ⇒ gradient agrees with the causal oracle.
- (B) INDEX/POSITION path — naive `∂pixel/∂ball_x = 0.0` (the documented vanishing), while
  the intervention oracle's finite Δ at the moved-ball footprint = 20.0 ⇒ **E1-1 is the
  sole truth** for position/index outputs.
- (C) bilinear sampler `∂pixel/∂ball_x = 166.0` ⇒ restores the position gradient (the
  documented workaround; NOT the oracle).
Self-check PASSES (content grad nonzero, index grad ~0, sampler restores it). Artifacts
(SPEC §R, siblings of the E1-1 records): `out/oracle_grad_pong.{json,npz}`.
Run: `julia --project=<primary>/jutari tools/xai_study/ground_truth/oracle_grad.jl`
(add `--selftest` for the assert-only fast path).
