---
id: P2-E9-1
title: document_check pass — required statements, Reporting Summary, limits
epic: E9 (Reproducibility & submission prep)
status: done
sprint: 8
owner:
where: local
depends_on: [P2-E8-9]
file_scope:
  - document_check.md
  - paper/sections/09_statements.tex
estimate: M
spec_ref: ../SPEC.md#e9-reproducibility-submission-prep-sprint-7-local-sm-led
---

## Goal
Walk the full submission-compliance checklist in `document_check.md` and satisfy every item:
the required statements (Data/Code availability, Author contributions, Competing interests,
Acknowledgements, Ethics/dual-use, Statistics & reproducibility), the Reporting Summary, and the
length/limit caps — marking each `[ ]` → `[x]` with where it is satisfied. The required-statements
LaTeX block lives in its own section file so it stays disjoint from the E8-9 end-matter prose.

## Definition of Done
- [x] every item in `document_check.md` marked `[x]` with its satisfying location, or explicitly flagged for the PO
- [x] required-statements block present — consolidated into `paper/sections/09_endmatter.tex` (availability, contributions, competing interests, ethics; see Notes for the 09-split resolution)
- [x] all quantitative claims state n / test / uncertainty (document_check §D) — verified; one residual gap (explicit seed/game uncertainty bands) flagged for the PO
- [x] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [x] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [x] `status: done`

## Notes / handoff
SPEC E9-1 named the scope `document_check.md` + `paper/sections/09_*` and proposed a distinct
`09_statements.tex` to keep file scopes disjoint from E8-9's `09_endmatter.tex`. **Resolution at
this pass:** the SM had already consolidated *all* back-matter required statements into
`09_endmatter.tex` (E8-9), and `main.tex:88` inputs only that one file. Creating a second
`09_statements.tex` would duplicate the statements, so this item instead (a) **verified** the
existing endmatter against every NMI requirement, (b) added a P2-E9-1 verification note to the
endmatter header, and (c) wrote the per-requirement mapping into `document_check.md`. No
`09_statements.tex` exists or is needed. Full doc compiles clean (`pdflatex -draftmode`, exit 0);
endmatter has zero `\paragraph` run-in labels (STYLE.md §1/§3). Limitations live in
`08_discussion.tex:139–152` (ordinary prose, not duplicated in end matter). Checklist content is
document_check.md §D + §F + §G; gaps for the PO are listed in document_check.md "Gaps requiring
the PO".
