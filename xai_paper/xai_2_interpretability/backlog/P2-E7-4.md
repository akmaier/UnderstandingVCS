---
id: P2-E7-4
title: Figure 4 — attribution (Phase B) vs mechanistic interp (Phase C)
epic: E7 (Figures)
status: done
sprint: 6
owner: agent-3
where: local
depends_on: [P2-E6-1]
file_scope:
  - paper/figures/fig4_attribution_vs_mechanistic.py
  - paper/figures/fig4_attribution_vs_mechanistic.pdf
estimate: M
spec_ref: ../SPEC.md#e7-figures-sprint-5-local-one-figure-per-item
---

## Goal
Render Figure 4: a head-to-head of Phase-B attribution methods against Phase-C mechanistic-interp
methods on the shared faithfulness metrics (corr + deletion/insertion AUC + precision@k for B;
recovered-vs-exact-patch / feature↔variable / scrubbing for C), showing where causal methods pass and
correlational/approximate ones fall short. Reads the leaderboard / results index.

## Definition of Done
- [x] runs to completion from this command: `python paper/figures/fig4_attribution_vs_mechanistic.py --leaderboard tools/xai_study/compare/out/leaderboard.json`
- [x] produces a vector PDF at `paper/figures/fig4_attribution_vs_mechanistic.pdf`, colour-blind-safe, self-contained legend, B vs C methods clearly grouped
- [x] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [x] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [x] `status: done`

## Notes / handoff
Metrics per experiment_design.md §5 (Phase B) and §6 (Phase C); the pass/fail prediction is §7
(method matrix). plan.md figure (4). Data-driven → depends on E6-1.

**Done (Sprint 6).** Filename uses the descriptive form `fig4_attribution_vs_mechanistic.{py,pdf}`
(matching fig1's precedent; `file_scope` above updated from the `fig_4.*` placeholder). Two-panel
landscape PDF, pure read over committed records (no experiment re-run):
- **(a) Phase B**, 12 methods, faithfulness (Pearson vs §1 oracle) **split by output regime**
  (content vs position/index). The whole gradient family — vanilla saliency, SmoothGrad, guided
  backprop, Grad×Input/DeepLIFT, Integrated Gradients, Expected Gradients (+ the gradient-leaning
  surrogates LIME/KernelSHAP/RISE) — **collapses to 0.00 on the position regime** while holding on
  content (0.53–0.73); the intervention/perturbation family (occlusion 0.66/0.31, on-distrib.
  counterfactual 0.56/0.14, extremal perturbation 0.49/0.20) **holds on position**. A dashed
  divider + tick-colour separates the two families.
- **(b) Phase C**, 10 methods, faithfulness vs exact patch / true data-flow with 95% CI whiskers.
  Activation patching, interchange/DAS, causal scrubbing, logit/tuned lens = **1.000 (oracle
  ceiling)**; path patching 0.46, ACDC 0.45 (partial); attribution patching 0.50 (gradient approx);
  and the **present-not-used** cluster — NMF/PCA 0.54, SAE 0.19, linear probing 0.16 — bracketed.
- **Headline banner** (from `faithful_demo.json`, P2-E6-3): position-regime causal/intervention
  bucket 0.412 (±0.390, n=4) vs gradient/correlational 0.068 (±0.070, n=9) → 0.344 gap;
  activation patching 1.000 vs vanilla saliency 0.000.
- In-script self-check: PDF exists / >4 KB / `%PDF-` header + 9 data-integrity assertions
  (12 B + 10 C methods, gradient-vanishes-on-position, intervention-holds, activation-patching==1.0,
  SAE+probing<0.25, headline gap==0.3435). ALL PASS.
