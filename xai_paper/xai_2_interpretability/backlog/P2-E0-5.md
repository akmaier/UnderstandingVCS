---
id: P2-E0-5
title: Backlog construction (Sprint 0)
epic: E0 (Infrastructure & harness bootstrap)
status: done
sprint: 0
owner:
where: local
depends_on: []
file_scope:
  - backlog/**
estimate: M
spec_ref: ../SPEC.md#e0--infrastructure--harness-bootstrap-sprint-01-mostly-local
---

## Goal
Generate one `backlog/<ID>.md` per SPEC task (schema SCRUM §3) and fill `backlog/BOARD.md`,
assigning each item a tentative sprint + disjoint file-scope and verifying per-sprint
disjointness (SCRUM §0). This is the Sprint 0 bootstrap that makes later sprints runnable.

## Definition of Done
- [ ] every SPEC task (E0–E9, incl. one item per named Phase-B/C method) has a `backlog/<ID>.md` with id/title/epic/status/sprint/owner/where/depends_on/file_scope/estimate/spec_ref
- [ ] `backlog/BOARD.md` lists every item with status, sprint, owner slot, file-scope, deps
- [ ] per-sprint file-scopes verified pairwise disjoint (SCRUM §0)
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
This item is itself executed across parallel Sprint-0 agents partitioned by epic (disjoint
file-scopes per agent: each agent writes only its `backlog/P2-E*-*.md`). BOARD.md is
SM-only — written/merged at Review, not by developer agents.
