---
id: P2-E2-2
title: Verify-by-intervention + offset-correct T3 labels
epic: E2 (T3 acquisition)
status: todo
sprint: 2
owner:
where: local
depends_on: [P2-E2-1, P2-E1-1]
file_scope:
  - tools/xai_study/t3/verify_labels.*
  - tools/xai_study/t3/out/verified_*
estimate: L
spec_ref: ../SPEC.md#e2--t3-acquisition-sprint-12-local
---

## Goal
Upgrade candidate T3 labels from correlational to verified-causal: perturb the candidate
RAM byte, re-render on the exact framebuffer, confirm the object moves to the predicted
place (auto-correcting render-time offsets), and record a per-game verified-rate.

## Definition of Done
- [ ] `verify_labels.*` sets each candidate byte, re-renders via the bit-exact framebuffer, and confirms predicted object motion (intervention test, strictly stronger than probing)
- [ ] render-time offsets auto-corrected during verification
- [ ] verified T3 labels for the core set + per-game verified-rate written to `out/verified_*` per SPEC §R
- [ ] runs from a documented command
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
experiment_design.md §2 step (2). Uses the intervention oracle (P2-E1-1). Thesis tie-in:
probing shows info present, not used (Hewitt & Liang control tasks); our intervention test
is the stronger verification. The verified-rate is a reportable number.
