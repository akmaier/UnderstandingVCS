---
id: P2-E0-2
title: State/trajectory recorder
epic: E0 (Infrastructure & harness bootstrap)
status: todo
sprint: 1
owner:
where: local
depends_on: [P2-E0-1]
file_scope:
  - tools/xai_study/common/record_state.*
estimate: M
spec_ref: ../SPEC.md#e0--infrastructure--harness-bootstrap-sprint-01-mostly-local
---

## Goal
Dump per-step RAM/registers/TIA-RIOT/opcode/framebuffer for a ROM + input-trace, reusing
the Paper-1 dump machinery, into a trajectory tensor that Phase A/C consume as
"activations". Must stay inside the conformance horizon (experiment_design.md §3 caveat ii).

## Definition of Done
- [ ] `tools/xai_study/common/record_state.*` produces a trajectory tensor (per-step RAM + registers + TIA/RIOT + opcode + framebuffer) for Space Invaders within the conformance horizon
- [ ] reuses Paper-1 dumps; does NOT modify the emulator core (SCRUM §7)
- [ ] documented: tensor layout, horizon bound, input-trace format; runs from a documented command
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
Conformance horizon ~30-frame RAM / 60-frame screen on a fixed action stream
(experiment_design.md §3). The recorded trajectory is the "activations" tensor for
Phase C (E5) and the state input for Phase A (E3).
