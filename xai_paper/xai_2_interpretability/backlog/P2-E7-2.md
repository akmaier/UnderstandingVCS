---
id: P2-E7-2
title: Figure 2 — A–C on the shared faithfulness-vs-plausibility axes (headline)
epic: E7 (Figures)
status: done
sprint: 6
owner:
where: local
depends_on: [P2-E6-1, P2-E6-3]
file_scope:
  - paper/figures/fig_2.py
  - paper/figures/fig_2.pdf
estimate: M
spec_ref: ../SPEC.md#e7-figures-sprint-5-local-one-figure-per-item
---

## Goal
Render Figure 2, the headline plot: every Phase-A/B/C method placed on the shared axes —
faithfulness (X, vs the oracle) vs human-plausibility (Y) — with the danger zone (high plausibility,
low faithfulness) marked. Reads the leaderboard (E6-1) and the faithful-method demonstration (E6-3);
this is the figure the whole paper builds toward.

## Definition of Done
- [ ] runs to completion from this command: `python paper/figures/fig_2.py --leaderboard tools/xai_study/compare/out/leaderboard.json`
- [ ] produces a vector PDF at `paper/figures/fig_2.pdf`, colour-blind-safe, with a self-contained legend, methods labelled by tradition (A/B/C)
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
Axes and danger-zone per experiment_design.md §0 (two reporting axes). Data-driven → strictly depends
on E6-1 (leaderboard) and E6-3 (the near-ceiling vs near-chance contrast). plan.md figure (2).

**Delivered (S6):** `paper/figures/fig2_faithfulness_vs_plausibility.{py,pdf}` (the sprint
brief's filenames, glob `fig2_*`; the `fig_2.*` names in `file_scope` above were the
placeholder). Scatter of all 30 leaderboard methods + the ORACLE ceiling at (1,1): X =
faithfulness vs the §1 oracle (leaderboard `faithfulness`), Y = plausibility proxy
(`plausibility_proxy`); colour = tradition (Okabe-Ito), marker = phase (A=○, B=□, C=△,
oracle=★). Danger zone (F≤0.40, P≥0.70) drawn as a wash; worst offenders annotated
(expected_gradients F=0.034, A3 tuning F=0.116, A6 Granger F=0.136, linear_probing
F=0.162, guided_backprop F=0.267) plus vanilla_saliency F=0.267; faithful cluster near
ceiling (activation_patching F=1.000, occlusion F=0.482, on-distribution counterfactual
F=0.350). E6-3 contrast arrow vanilla_saliency→activation_patching (0.267→1.000; position
regime 0→1). Runs argument-free or with `--leaderboard …/leaderboard.json`; vector PDF
(0 raster images, 18 embedded fonts); in-script self-check passes (%PDF- header, 188 KB,
contrast endpoints cross-checked equal between leaderboard and faithful_demo). Every
plotted value traces to a committed record; no experiment re-run.
