---
id: P2-E4-12
title: On-distribution counterfactual attribution vs oracle
epic: E4 (Phase B)
status: done
sprint: 5
owner:
where: local
depends_on: [P2-E4-0]
file_scope:
  - tools/xai_study/phaseB_attribution/counterfactual.jl
  - tools/xai_study/phaseB_attribution/out/counterfactual_*
estimate: L
spec_ref: ../SPEC.md#e4--phase-b-attribution--xai
---

## Goal
Implement on-distribution counterfactual attribution (state-set + re-render; cf. Olson 2021;
Atrey 2020) — find the minimal valid counterfactual edit that flips/changes the chosen
output `y` — on the core game set, scored against the §1 oracle. Because we set state and
re-render exactly, this removes Atrey's off-manifold objection (method matrix §7: expected
Partial→Succeed).

## Definition of Done
- [x] runs to completion: `julia --project=<repo>/jutari tools/xai_study/phaseB_attribution/counterfactual.jl --games core` (substrate pivot to JULIA — the jutari real-ROM fast path, like every Phase-B sibling on main; the Python path is the deferred cluster GPU batch)
- [x] result written to `tools/xai_study/phaseB_attribution/out/counterfactual_*` in the schema (SPEC §R) — 6 games × {content, position} + core summary (13 JSON + 12 NPZ)
- [x] metrics recorded: validity + minimality/sparsity vs the true (single-cell oracle) causal support; faithfulness (corr / del-ins-AUC-on-the-true-VCS / precision@k) vs the intervention oracle; oracle-as-method positive control. (Object-level T3 metrics are out of scope here — pixel/cell level per the §5 row's "pixel-level: no T3".)
- [x] nothing outside `file_scope` changed; Paper-1 gates untouched (no jutari/jaxtari/xitari core touched)
- [x] committed + pushed to main (rebase-before-push)
- [x] `status: done`

## Notes / handoff
experiment_design.md §5 (row "On-distribution counterfactual") + §7. Object-level
counterfactuals **need T3**; pixel-level does not. Uses set-state-and-re-render via the
intervention oracle (E1-1) machinery.

**Built** (`counterfactual.jl`, Julia, reuses `ig_baseline_sweep.jl` env+scorer +
`oracle_intervene.jl` causes + `jutari_oracle.jl` re-run/NPZ). The method finds the
MINIMAL subset S of candidate RAM cells whose substitution toward a REAL on-distribution
alternative state x' (genuine RAM at another reachable frame of the same ROM) + a re-run
of the real ROM moves the output y by ≥ τ·|y(x')−y(x)| (greedy forward + backward prune,
all real re-runs). Set-state-and-re-render keeps every edit on-distribution ⇒ removes
Atrey 2020's off-manifold objection (§7 Partial→Succeed). Per-cell CF attribution scored
vs the oracle on the §5 metrics.

**Results (content path, τ=0.5, f120+30):** valid on-distribution counterfactual found
for 5/6 games, each a SINGLE-cell minimal flip, with strong oracle faithfulness —
qbert corr=0.86 p@3=1.0 (RAM[46] 65→41), ms_pacman 0.82 p@3=1.0 (RAM[6] 88→104),
seaquest 0.76 p@3=1.0 (RAM[1] 212→16), pong 0.71 p@3=0.67 (RAM[54] 124→0, |S|=1/2
causal cells → sparsity 0.5), space_invaders 0.22 (RAM[81] 69→60). breakout = the honest
"no causally-active candidate byte VARIES under a NOOP policy ⇒ no reachable
counterfactual" case (`varies=false`, valid=false). Oracle-as-method positive control
corr=1/p@3=1 on every game. §7 CONTRAST: the counterfactual produces a real attribution
on POSITION outputs (it re-runs the real ROM, no gradient) where IG/saliency VANISH (e.g.
pong ball-pixel corr=0.84 via single-cell substitution).
