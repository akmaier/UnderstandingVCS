---
id: P2-E7-1
title: Figure 1 — platform & ground-truth oracle schematic
epic: E7 (Figures)
status: done
sprint: 6
owner:
where: local
depends_on: []
file_scope:
  - paper/figures/fig1_platform_oracle.py
  - paper/figures/fig1_platform_oracle.pdf
estimate: S
spec_ref: ../SPEC.md#e7-figures-sprint-5-local-one-figure-per-item
---

## Goal
Render Figure 1: the platform-and-oracle schematic. Show the VCS substrate (jutari/jaxtari: exact
forward, exact interventions, full observability, content-path gradients) and how the ground-truth
oracle is constructed — intervention (occlude/clamp/replace/resample → bit-exact re-run → Δy) plus
the content-path gradient companion. Schematic (no experiment data), so it has no E6 dependency.

## Definition of Done
- [ ] runs to completion from this command: `python paper/figures/fig_1.py`
- [ ] produces a vector PDF at `paper/figures/fig_1.pdf`, colour-blind-safe, with a self-contained legend
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
Content per plan.md figure list (1) and experiment_design.md §1 (oracle) + §3 (substrate audit).
Make the intervention-vs-gradient (content-path-only) distinction visually explicit — it recurs as a
caveat throughout B/C.

**Done (P2 S6).** Output filenames follow the Sprint-6 task brief (`fig1_platform_oracle.{py,pdf}`),
not the original `fig_1.*` placeholder — `file_scope` above updated to match. Three-stage left→right
schematic: (1) differentiable VCS substrate (jutari/jaxtari, bit-exact to xitari, Paper-1 64/64; four
capabilities: exact forward / exact interventions / full observability / content-path gradients) →
(2) ground-truth oracle = the exact intervention oracle `do(u:=v′)→bit-exact re-run→Δy(u)` (PRIMARY)
+ the content-path gradient companion `∂y/∂u` via SOFT-STE, with the content-valid vs index-zero
caveat drawn explicitly (for position/index/event outputs the intervention oracle is the SOLE truth;
gradients are scored as methods under test) → (3) F∧S∧M correctness triad (§0) scoring any XAI method,
with the faithfulness-vs-plausibility danger-zone callout. Vector PDF, Okabe-Ito colour-blind-safe,
self-contained 5-row legend + provenance footnote. Schematic only (no experiment data → no E6 dep).
Self-check in-script: PDF exists, `%PDF-` header, >4 KB (got ~206 KB, embedded TrueType fonts, vector).
Run: `python paper/figures/fig1_platform_oracle.py` (jaxtari/.venv python, matplotlib 3.11).
