---
id: P2-E3-2
title: A2 single-unit lesions importance map
epic: E3 (Phase A — neuroscience battery)
status: done
sprint: 5
owner:
where: local
depends_on: [P2-E3-0]
file_scope:
  - tools/xai_study/phaseA_kording/A2_lesions.py
  - tools/xai_study/phaseA_kording/out/A2_*
estimate: L
spec_ref: ../SPEC.md#e3--phase-a-neuroscience-battery-sprints-23-local-pilot-cluster-full
---

## Goal
Produce a per-unit importance map by lesioning each state unit (does boot/run still
work?), then score the importance ranking against each unit's true causal role and count
units flagged "specific" that are actually generic.

## Definition of Done
- [ ] `A2_lesions.py` lesions each unit and records a per-unit importance map over the core set
- [ ] scored: rank-correlation of importance with the unit's true role + #units flagged "specific" that are generic, written to `out/A2_*` per SPEC §R
- [ ] submitted as a cluster array sweep (record Slurm job id in this item file); results collected at Review
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
experiment_design.md §4 A2. Ground truth T1 causal role (naming optional). Lesion sweeps =
cluster (submit via E0-3, do not hold a worker, SCRUM §9.1). Serialize against other heavy
cluster sweeps in the same slot.

### Done (P2 S5, jutari/Julia path)
- Implemented `phaseA_kording/A2_lesions.jl` (Julia, matching the A1..A8 sibling
  pattern — `.jl` not `.py`; the shared lib + all other Phase-A runners are Julia,
  jaxtari being the cluster-only GPU path, SCRUM §7). Cluster-shardable:
  `--game G` / `--shard i --nshards n --shard-kind game` + `--out-dir` + `--roms-dir`
  + `--where` (so `tools/cluster/xai_array_jl.sbatch` fans one game per array task;
  shard smoke-tested). Default (no args) = all 6 core games locally.
- Method: EXHAUSTIVE single-unit lesions — clamp each candidate RAM cell to {0, alt}
  one at a time, re-run the real ROM (bit-exact), score the per-unit importance map
  vs the exact oracle's per-unit causal role. Reuses `common/jutari_oracle.jl`
  (boot/replay/snapshot/deepcopy-checkpoint/intervene/NPZ) + the candidates map.
- RAN SYNC, all 6 core games (frame 30 + horizon 30, inside the conformance window),
  positive control ρ=1.0 on every game, all self-checks PASS:
  - pong ρ=1.0, breakout ρ=0.97, space_invaders ρ=0.98, seaquest ρ=1.0,
    ms_pacman ρ=1.0, qbert ρ=1.0 (mean ρ=0.991). **HIGH F as predicted** —
    single-unit ablation ≈ the causal map.
  - Where single-unit lesions MISS interaction effects (the blind spot): missed-
    interaction pairs/game = pong 3/28, breakout 1/36, space_invaders 22/55,
    seaquest 8/55, ms_pacman 2/55, qbert 3/55; max super-additive Δ up to 1044
    (space_invaders) — super/sub-additivity no single-unit map can represent.
  - Spurious specificity (#flagged-specific that are actually generic): up to 2/8
    (space_invaders, M=0.75), 1/15 seaquest, 1/7 ms_pacman.
  - Budget recorded in each §R record (2 lesion clamps + 4 oracle doses + 1 held-out
    dose per unit; ≤60 interaction pairs; all-NOOP trace; incremental from a cached
    boot+replay checkpoint).
- Artifacts: `phaseA_kording/out/A2_lesions_<game>.{json,npz}` (6) + combined
  `A2_lesions.{json,npz}`. NPZ verified numpy-loadable.
- Cluster array submission (Slurm job id) deferred to E0-3 batch / Review — the
  runner is array-ready; local 6/6 already covers the core-set DoD.
