---
id: P2-E0-3
title: Cluster job templates
epic: E0 (Infrastructure & harness bootstrap)
status: todo
sprint: 0
owner:
where: local
depends_on: []
file_scope:
  - tools/cluster/xai_*.sbatch
  - tools/xai_study/common/cluster.md
estimate: S
spec_ref: ../SPEC.md#e0--infrastructure--harness-bootstrap-sprint-01-mostly-local
---

## Goal
Author Slurm templates `tools/cluster/xai_*.sbatch` for CPU array sweeps and GPU batches
(mirroring the Paper-1 pattern) plus a `cluster.md` documenting the LME workflow, so later
cluster-tagged experiments have a ready, validated launch path.

## Definition of Done
- [ ] `tools/cluster/xai_*.sbatch` templates exist for (a) CPU array sweeps and (b) GPU batches
- [ ] a dry-run job script validated (`sbatch --test-only` or equivalent)
- [ ] `tools/xai_study/common/cluster.md` documents `/cluster/maier`, jaxtari GPU venv (`jax[cuda12]`), the one-SSH-per-wakeup rule, and ROM availability under `/cluster/maier`
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
Mirror Paper-1 `tools/cluster/*.sbatch`. Heavy gates run alone; serialize contending GPU
jobs (SCRUM §7). Used by all `where: cluster` items in E3/E4/E5.
