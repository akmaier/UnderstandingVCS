---
id: P2-E0-3
title: Cluster job templates
epic: E0 (Infrastructure & harness bootstrap)
status: done
sprint: 0
owner:
where: local
depends_on: []
file_scope:
  - tools/cluster/xai_*.sbatch
  - tools/xai_study/common/cluster.md
estimate: S
spec_ref: ../SPEC.md#e0--infrastructure--harness-bootstrap-sprint-01-mostly-local
---

## Goal
Author Slurm templates `tools/cluster/xai_*.sbatch` for CPU array sweeps and GPU batches
(mirroring the Paper-1 pattern) plus a `cluster.md` documenting the LME workflow, so later
cluster-tagged experiments have a ready, validated launch path.

## Definition of Done
- [ ] `tools/cluster/xai_*.sbatch` templates exist for (a) CPU array sweeps and (b) GPU batches
- [ ] a dry-run job script validated (`sbatch --test-only` or equivalent)
- [ ] `tools/xai_study/common/cluster.md` documents `/cluster/maier`, jaxtari GPU venv (`jax[cuda12]`), the one-SSH-per-wakeup rule, and ROM availability under `/cluster/maier`
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
Mirror Paper-1 `tools/cluster/*.sbatch`. Heavy gates run alone; serialize contending GPU
jobs (SCRUM §7). Used by all `where: cluster` items in E3/E4/E5.

**Delivered (P2 S1):**
- `tools/cluster/xai_array.sbatch` — CPU **array** sweep, one Slurm array task per shard
  (game/state/lesion); runner-agnostic via `XAI_CMD`/`XAI_ARGS`; shard passed both as env
  (`SLURM_ARRAY_TASK_ID`/`NSHARDS`/`SHARD_KIND`) and CLI (`--shard/--nshards/--shard-kind`);
  `%K` array cap throttles contention. 4 cpus, 16G, 4h.
- `tools/cluster/xai_gpu.sbatch` — single-GPU batch (IG-over-ROM / batched gradients / SAE),
  mirrors `bench_gpu.sbatch` (jaxtari `jax[cuda12]` venv, `jax.devices()` GPU assertion,
  shared `/cluster/maier/.jax_cache`, `nvidia-smi` non-fatal). `--gres=gpu:<type>:1`, 32G, 8h.
- `tools/xai_study/common/cluster.md` — submit/collect doc: `/cluster/maier`, GPU venv, ROM
  path, one-SSH rule, shared-runner flag contract (SPEC §R out dirs), SCRUM §7 gotchas.

**Validated (authoring only — no submission):** `bash -n` clean on both; `#SBATCH` header
lint PASS (required directives present, valid `--time`, no unknown directives); dry-run of
the script bodies confirms the env guards fire (missing `XAI_CMD` → FATAL; bogus runner →
"runner not found" after the shard banner) and shard→flag resolution is correct.

**ENV blocker (expected, per item DoD):** Slurm is **not installed on the dev laptop**
(`sbatch`/`squeue`/`scontrol` absent), so the spec'd `sbatch --test-only` could not be run
here. Substituted `bash -n` + a Python `#SBATCH` header lint + a no-Slurm dry-run of the
body. Run `sbatch --test-only tools/cluster/xai_array.sbatch` (and `xai_gpu.sbatch`) on an
LME login node as `maier` to validate the headers against the live partitions before the
first real submit.
