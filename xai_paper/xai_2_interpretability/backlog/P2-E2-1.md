---
id: P2-E2-1
title: Import OCAtari/AtariARI candidate labels
epic: E2 (T3 acquisition)
status: done
sprint: 1
owner: agent-2
where: local
depends_on: []
file_scope:
  - tools/xai_study/t3/import_labels.*
  - tools/xai_study/t3/out/candidates_*
estimate: M
spec_ref: ../SPEC.md#e2--t3-acquisition-sprint-12-local
---

## Goal
Import OCAtari (RAM mode + Vision mode) and AtariARI candidate RAM→game-concept maps for
the core game set, producing candidate (correlational) T3 labels that the verify step will
upgrade to causal.

## Definition of Done
- [ ] `import_labels.*` produces candidate RAM→concept maps for the core set, sourced from OCAtari (40+ games) and AtariARI (22 games)
- [ ] candidates written to `tools/xai_study/t3/out/candidates_*` with provenance (source, raw vs offset-applied) per SPEC §R
- [ ] runs from a documented command
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
experiment_design.md §2: AtariARI raw (x,y) is NOT aligned to rendered position
(render-time offsets); OCAtari RAM mode applies offsets. These are candidates only —
correlational until verified by intervention (P2-E2-2). Feeds the game-set audit (P2-E0-4).

### Done (agent-2, Sprint 1)
- Built `tools/xai_study/t3/import_labels.py`. Two sources merged per game with full
  provenance per SPEC §R:
  - **AtariARI**: `ram_annotations` (atari_dict) transcribed verbatim for the 6 core
    games — RAW RAM indices, `offset_applied:false` (AtariARI does not offset-correct).
  - **OCAtari**: AST-parsed from the installed `ocatari/ram/<game>.py` extractors
    (top-level `import ocatari` needs ALE+ROMs, so the pure-Python ram modules are
    parsed, not imported). Recovers `ram_state[i] (+/- offset)` -> concept with the
    render-time offset captured (e.g. Pong ball.xy = ram[49]-49, ram[54]-14).
- Run command (ran CLEAN, exit 0):
  ```
  PY=/Users/maier/Documents/code/UnderstandingVCS/jaxtari/.venv/bin/python
  OCSRC=$($PY -c 'import sysconfig,os;print(os.path.join(sysconfig.get_paths()["purelib"],"ocatari"))')
  PYTHONPATH=<worktree> $PY tools/xai_study/t3/import_labels.py --ocatari-src "$OCSRC"
  ```
- Artifacts: `tools/xai_study/t3/out/candidates_{pong,breakout,space_invaders,seaquest,ms_pacman,qbert}.json`
  Candidate counts (AtariARI / OCAtari / total): pong 8/14/22, breakout 6/10/16,
  space_invaders 7/16/23, seaquest 11/35/46, ms_pacman 17/31/48, qbert 12/16/28.
  All 6 validate against the SPEC §R header + per-candidate provenance
  (`source`, `raw`, `offset_applied`, `offset`). Both sources present in every game.
- ENV note (handled, no leftover damage): `pip install ocatari` into the shared jaxtari
  venv pinned numpy<2.0 and DOWNGRADED numpy to 1.26.4, which breaks jax (needs >=2.0).
  I reinstalled `numpy>=2.1` (now 2.4.6) and verified jax 0.10.1 imports + runs.
  ocatari's own `numpy<2.0` pin is now an unsatisfied soft constraint, but it is only
  read for source (never imported at runtime here), so jax/jaxtari is unaffected.
  If a future task imports ocatari at runtime, do it in a separate venv to avoid the
  numpy conflict.
- For SM: candidate counts feed the game-set / T3-coverage audit (P2-E0-4). Verification
  by intervention (upgrade correlational -> causal, offset-correct) is P2-E2-2.
