---
id: P2-E3-0
title: Phase-A pilot (A2+A3+A7 on Space Invaders, scored)
epic: E3 (Phase A — neuroscience battery)
status: done
sprint: 3
owner:
where: local
depends_on: [P2-E1-1, P2-E0-2j]
file_scope:
  - tools/xai_study/phaseA_kording/pilot_si.*
  - tools/xai_study/phaseA_kording/out/pilotA_*
estimate: M
spec_ref: ../SPEC.md#e3--phase-a-neuroscience-battery-sprints-23-local-pilot-cluster-full
---

## Goal
Run the Phase-A pilot on Space Invaders: lesions (A2), tuning curves (A3) and
dim-reduction (A7), each scored against the oracle, to de-risk the battery and the scoring
harness before the full core-set fan-out.

## Definition of Done
- [x] `pilot_si.*` runs A2 + A3 + A7 on Space Invaders and produces pilot F/S/M scores
- [x] scores written to `out/pilotA_*` per SPEC §R, referencing the oracle (P2-E1-1)
- [x] runs to completion locally from a documented command
- [x] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [x] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [x] `status: done`

## Notes / handoff
experiment_design.md §4 (A2 lesions, A3 tuning, A7 dim-reduction). Pilot gates the full
E3-1..E3-8 fan-out (their dep). Phase A is the calibration baseline (classical methods
low-F), not the headline (experiment_design.md §4 Novelty note).

### DONE (Sprint 3)
Built `tools/xai_study/phaseA_kording/pilot_si.jl` (pure Julia, on the verified jutari
foundation: `jutari_oracle.jl` + `oracle_intervene.jl` machinery + `jutari_record.jl`).
NO emulator core touched.

**Run (executes on jutari, ~30 s):**
```
julia --project=/Users/maier/Documents/code/UnderstandingVCS/jutari \
      tools/xai_study/phaseA_kording/pilot_si.jl            # writes the artifact
julia --project=.../jutari tools/xai_study/phaseA_kording/pilot_si.jl --selftest  # self-check only
```

**Setup:** Space Invaders, tf=30 + horizon=30 (total 60 = SI 60-frame screen conformance
window), 4×FIRE→NOOP trace (bit-exact re-run asserted). 17 candidate cells from
`t3/out/candidates_space_invaders.json`. A3/A7 use a 150-frame ACTIVE trajectory
(FIRE + periodic RIGHTFIRE/LEFTFIRE) — a descriptive jutari recording, not a
bit-exact-vs-xitari claim — because the candidate cells are nearly static under the
in-window NOOP tail.

**Key results (scored vs the intervention oracle = P2-E1-1):**
- A2 lesions: 10/17 cells genuinely causal (oracle); TRIAD **F=0.961, S=0.688, M=1.0**
  (lesions ARE interventions ⇒ high F — A2 is the high-faithfulness anchor of the battery).
- A3 tuning: 5 strongly-tuned cells (selectivity≥0.7 to a game concept, frame-clock excluded
  as control), **spurious-tuning rate=0.6** (3/5 strongly-tuned cells are NOT causal — the
  present≠used trap; spurious = RAM[81]bullets, RAM[87]xy, RAM[120]visible).
- A7 PCA (LinearAlgebra.svd): top-1 PC=52.4% var, top-5=94.3%, **matched-component
  fraction=0.0** (no leading PC loads mainly on the oracle-causal cells — PCA captures
  epiphenomenal structure, not the causal variables). The quantified-Kording lesson.

**Harness positive control:** scoring the oracle's OWN causal map as the candidate method
yields F=1.0 (the self-check asserts this) — so the sub-1 A3/A7 numbers are real measurements.

**Artifact (SPEC §R, oracle_ref set):**
`tools/xai_study/phaseA_kording/out/pilotA_space_invaders.{json,npz}` — validated:
JSON parses, NPZ loads in numpy (12 arrays, PCA var sums to 1.0).

**Self-check:** the `--selftest` flag (embedded in `pilot_si.jl`, in file_scope) asserts
bit-exactness, ≥1 oracle-causal cell, the F==1 positive control, triad ranges, and
A3/A7 well-formedness. PASS.

**Gates the E3-1..E3-8 fan-out** (full A1..A8 battery over the core set; cluster for sweeps,
forward bit-exact to this HARD path). The OracleIntervene module is Pong-specialised, so
SI outputs/causes are defined inline here using the SAME bit-exact primitives — E3-1..E3-8
should factor a game-agnostic outputs/causes layer when they generalise beyond SI/Pong.
