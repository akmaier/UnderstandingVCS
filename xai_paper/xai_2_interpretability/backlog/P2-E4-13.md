---
id: P2-E4-13
title: N/A audit — Grad-CAM/attention/VIPER do-not-apply writeup
epic: E4 (Phase B)
status: done
sprint: 4
owner:
where: local
depends_on: [P2-E4-0]
file_scope:
  - tools/xai_study/phaseB_attribution/na_audit.jl
  - tools/xai_study/phaseB_attribution/na_audit.md
  - tools/xai_study/phaseB_attribution/out/na_audit_*
estimate: S
spec_ref: ../SPEC.md#e4--phase-b-attribution--xai
---

## Goal
Produce the recorded "does-not-apply" finding for methods that require NN layers / a learned
policy: Grad-CAM and Grad-CAM++ (Selvaraju 2017; Chattopadhyay 2018), attention rollout
(Abnar & Zuidema 2020), and VIPER (Bastani 2018). These have no conv maps / attention /
policy to attach to on the bare VCS, so the deliverable is a structured N/A audit (one entry
per method, with the missing-ingredient reason) rather than a score.

## Definition of Done
- [x] runs to completion from this command: `julia --project=<repo>/jutari tools/xai_study/phaseB_attribution/na_audit.jl` (Julia path — experiments run on jutari locally per SCRUM §5/§7; `--selftest` runs the self-check, `--md` (re)writes the writeup)
- [x] result written to `tools/xai_study/phaseB_attribution/out/na_audit_*` in the schema (SPEC §R) with method, applies=false, missing-ingredient reason per row (6 per-game `na_audit_<game>.{json,npz}` + `na_audit_combined.json`)
- [x] nothing outside `file_scope` changed; Paper-1 gates untouched/green (emulator core untouched; no shared-venv pip)
- [x] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [x] `status: done`

## Notes / handoff
experiment_design.md §5 (row "N/A") + §7 (last row). Recorded as a finding; feeds the
"architecture-specific methods don't apply" thread of the leaderboard (E6).

**Delivered (Julia / jutari, P2 S4):** `na_audit.jl` (runnable generator + self-check) +
`na_audit.md` (the writeup). Audits **6** N/A methods: Grad-CAM, Grad-CAM++, attention
rollout, VIPER, CAM, feature-visualisation/CAV — each `applies=false`,
`present_in_vcs=false`, with the missing substrate ingredient
(conv_feature_maps | attention | learned_policy | learned_channels) and a mechanistic
"why-not-this-method-on-VCS-state" note. The **finding is the asymmetry**: applicable
methods (IG, occlusion, vanilla-grad, counterfactuals) have ≥16 candidate CAUSE cells per
game (loaded from `t3/out/candidates_<game>.json`: pong 22 / breakout 16 /
space_invaders 23 / seaquest 46 / ms_pacman 48 / qbert 28) and ARE scored vs the §1
oracle; the N/A methods have **zero** of their required channels — structural, game-
independent. No F/S/M / positive control here (no method yields an importance claim to
score). `.npz` carries the all-zero missing-ingredient channel counts + applicable-cause
count so E6 reads the asymmetry without prose. Self-check (DoD): grounding round-trips,
all-false finding, §R keys present — PASS. **File-scope note:** delivered as `.jl`+`.md`
(not the originally-stubbed `na_audit.py`) since the prompt mandated Julia/jutari.
