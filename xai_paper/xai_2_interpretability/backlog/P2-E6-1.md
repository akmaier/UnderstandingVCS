---
id: P2-E6-1
title: Cross-tradition leaderboard (faithfulness vs plausibility)
epic: E6 (Cross-tradition comparison)
status: todo
sprint: 5
owner:
where: local
depends_on: [P2-E3-0, P2-E4-0, P2-E5-0]
file_scope:
  - tools/xai_study/compare/leaderboard.py
  - tools/xai_study/compare/out/leaderboard.*
estimate: M
spec_ref: ../SPEC.md#e6-cross-tradition-comparison-benchmark-artifact-sprint-5-local-sm-integrated
---

## Goal
Read the append-only `results_index.csv` (SPEC §R) and place every Phase-A/B/C method on the
two shared reporting axes — faithfulness (X, vs the §1 oracle ground truth) vs human-plausibility
(Y) — producing one comparable F∧S(∧M-where-available) score per method plus a discrepancy table
that highlights the danger zone (high plausibility, low faithfulness). This is the analytical core
the headline figure (E7) reads from; it must be a pure read over the results index (no re-running
experiments).

## Definition of Done
- [ ] runs to completion from this command: `python tools/xai_study/compare/leaderboard.py --index tools/xai_study/results_index.csv`
- [ ] result written to `tools/xai_study/compare/out/leaderboard.{json,csv}` in the schema (SPEC §R), with one F/S/M row per method and the plausibility-vs-faithfulness discrepancy table
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
Axes and the danger-zone framing come from experiment_design.md §0 (correctness triad → two reporting
axes) and the §9 master table (per-phase score columns). Faithfulness is scored against the §1
oracle, never against another interpretability method. Reads results only — does not write
`results_index.csv` (that is SM-merged at Review).
