---
id: P2-E5-7
title: NMF/PCA dictionaries — matched-component fraction
epic: E5 (Phase C)
status: done
sprint: 5
owner:
where: local
depends_on: [P2-E5-0]
file_scope:
  - tools/xai_study/phaseC_mechanistic/dictionaries.jl
  - tools/xai_study/phaseC_mechanistic/out/dictionaries_*
estimate: M
spec_ref: ../SPEC.md#e5--phase-c-mechanistic-interpretability
---

## Goal
Implement NMF/PCA dictionary learning over recorded VCS state trajectories, on the core game
set, recovering latent components and scoring the matched-component fraction against the
known variables (clocks, R/W signals, vsync; game variables where labeled). A linear-
dictionary baseline for the SAE (E5-6).

## Definition of Done
- [ ] runs to completion from this command: `python tools/xai_study/phaseC_mechanistic/nmf_pca.py --games core`
- [ ] result written to `tools/xai_study/phaseC_mechanistic/out/nmf_pca_*` in the schema (SPEC §R)
- [ ] metrics recorded: matched-component fraction vs known variables
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
experiment_design.md §6 (row "NMF/PCA dictionaries"). Hardware-variable matching uses T1/T2;
**game-variable matching needs T3**. Trajectories from E0-2.

### Done (commit pending)
- **File-scope note:** the dispatch prompt overrode the planning-time scope — the method
  needs an EMULATOR-touching CAUSAL-USE test (ablate an atom, re-run the real ROM via the
  oracle), so it is JULIA (`dictionaries.jl`, out `dictionaries_*`), not the planning-time
  `nmf_pca.py`. Scope updated above; nothing else touched.
- **Built** `tools/xai_study/phaseC_mechanistic/dictionaries.jl` — PCA (SVD) + NMF
  (Lee–Seung) + a small inline L1 SAE (manual GD, Julia) over the recorded (T,128) RAM
  trajectory; a component-count (rank) sweep {8,4,12}; the **matched-component fraction**
  vs known variables (T3 concept families + T2 clock/R-W/vsync) for all 3 methods; and the
  **causal-use test** — ablate a dictionary atom, reconstruct the state, write it into the
  real env and re-run via the §1 oracle, measuring Δscreen vs the reconstruct-only control.
  F/S/M triad (matched⇒causal?) scored against the exact oracle. Generalises A7 (Phase A
  dim-reduction) with causal scoring; contrasts NMF vs PCA vs SAE (E5-6).
- **Cluster-shardable:** `--game` / `--shard i --nshards n --shard-kind game` / `--out-dir`
  / `--roms-dir` / `--where`.
- **RAN** synchronously, all 6 core games, self-check PASS each, bit-exact re-run PASS each.
  Budget: ~107 s wall (M1 Max); 3 ranks × 3 methods × 6 games; SAE 3000 epochs (manual GD);
  NMF 300 iters; causal-use = (rank+1) full ROM re-runs/method/rank.
- **Headline (rank 8) matched-component fraction P/N/S + causal atoms P/N/S:**
  pong 0.38/0.62/0.38, 5/8/3 · breakout 0.50/0.62/0.50, 1/8/1 · space_invaders
  0.75/0.62/0.75, 4/8/1 · seaquest 0.50/0.62/0.38, 5/8/2 · ms_pacman 0.12/0.50/0.50, 7/8/2 ·
  qbert 0.50/0.25/0.50, 7/8/1. Positive control (all-atoms-ablated Δscreen) fires every game
  (8–1656 px). The dictionary trap shows: NMF parts span the varying subspace (all 8/8 atoms
  causal) while the sparse SAE names atoms that are mostly causally inert (1–3/8) — present ≠
  used at the feature level.
- **Artifacts:** `out/dictionaries_<game>.{json,npz}` (6 each) + `out/dictionaries_core_summary.json`.
