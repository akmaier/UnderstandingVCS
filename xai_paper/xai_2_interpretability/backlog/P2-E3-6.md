---
id: P2-E3-6
title: A6 Granger causality on CPU/TIA/RIOT
epic: E3 (Phase A — neuroscience battery)
status: done
sprint: 4
owner:
where: local
depends_on: [P2-E3-0]
file_scope:
  - tools/xai_study/phaseA_kording/A6_granger.jl
  - tools/xai_study/phaseA_kording/out/A6_*
estimate: M
spec_ref: ../SPEC.md#e3--phase-a-neuroscience-battery-sprints-23-local-pilot-cluster-full
---

## Goal
Infer CPU/TIA/RIOT causal edges via Granger causality and score the false-edge and
missed-edge rates against the true data-flow.

## Definition of Done
- [x] `A6_granger.jl` infers CPU/TIA/RIOT causal edges on the core set
- [x] scored: false-edge / missed-edge rate vs true data-flow, written to `out/A6_*` per SPEC §R
- [x] runs from a documented command (analysis = local)
- [x] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [x] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [x] `status: done`

## Notes / handoff
experiment_design.md §4 A6. Ground truth T1 (no T3). The true data-flow is the same
reference graph used by A1/A4; Granger is the classical (often misleading) contrast.

DONE. Implemented as Julia (`A6_granger.jl`) to match the on-main A-battery
convention (A1/A3/A4 are all `.jl`; the project runs on JUTARI locally) — the
file_scope conflict-key is the "A6 files", kept disjoint. Bivariate lag-2 Granger
(log-likelihood-ratio G = ln(RSS_r/RSS_u), Geweke 1982) with a dependency-free OLS
+ F-test p-value (regularized incomplete beta). Edge rule = canonical Granger
significance (F-test p ≤ alpha=0.05; optional magnitude floor granger_tau, default
0 = significance alone). TWO scopes: (1) SUBSYSTEM — CPU/TIA/RIOT per-frame activity
(#bytes changed) vs the T2 known data-flow (CPU→TIA, RIOT→CPU); (2) CELL — pairwise
Granger over candidate cells vs the EXACT intervention true data-flow graph (A1's
construction, exhaustive do-set, bit-exact re-run). Headline §4 metric = cell-scope
false-edge / missed-edge rate + a Granger-specific `direction_disagreement`
(temporal precedence vs true-causation arrow). F/S/M + positive control
(true-graph-as-method → false-edge=missed-edge=0, F1=1) per pilot_si.jl. Self-check
passes on all 6; 6/6 games scored.

Run: `XAI_PRIMARY_REPO=/Users/maier/Documents/code/UnderstandingVCS julia
--project=/Users/maier/Documents/code/UnderstandingVCS/jutari
tools/xai_study/phaseA_kording/A6_granger.jl`

Per-game cell-scope false-edge / missed-edge / dir-disagree (the quantified-Kording
result — Granger high false+missed despite fully-known structure):
  pong FE=1.00 ME=1.00 DD=0.00 (F=0.00); breakout FE=0.33 ME=0.60 DD=0.00 (F=0.50);
  space_invaders FE=0.91 ME=0.96 DD=0.00 (F=0.06); seaquest FE=0.98 ME=0.96 DD=1.00
  (F=0.03); ms_pacman FE=0.83 ME=0.67 DD=0.00 (F=0.22); qbert FE=1.00 ME=0.00 DD=0.00
  (F=0.01 — Granger fires 183 edges vs 1 true: massive over-claim). Positive control
  F1=1.0 on all 6.
