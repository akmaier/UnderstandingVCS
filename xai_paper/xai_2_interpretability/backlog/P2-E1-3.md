---
id: P2-E1-3
title: Oracle cross-check + validation
epic: E1 (Ground-truth oracle)
status: done
sprint: 1
owner:
where: local
depends_on: [P2-E1-1, P2-E1-2]
file_scope:
  - tools/xai_study/ground_truth/oracle_xcheck.*
  - tools/xai_study/ground_truth/out/oracle_xcheck_*
estimate: M
spec_ref: ../SPEC.md#e1--ground-truth-oracle-sprint-1-local-the-foundation
---

## Goal
Cross-check the intervention oracle against the gradient oracle on the content path:
report their correlation, flag non-smooth / saturated / index points where they disagree
(disagreement reported, not hidden), and produce the (1)↔(2) validation report that
declares the oracle the reference for Phases A/B/C.

## Definition of Done
- [x] `oracle_xcheck.*` reports intervention↔gradient correlation on the Pong content path
- [x] non-smooth / saturated / index disagreement points flagged and listed
- [x] the (1)↔(2) report written to `out/oracle_xcheck_*` in SPEC §R schema; the oracle is declared the A/B/C reference
- [x] runs from a documented command
- [x] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [x] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [x] `status: done`

## Notes / handoff
experiment_design.md §1 "Cross-check". Closes the oracle sprint barrier: oracle validated.
Disagreement at index/saturated points is expected and is itself a finding (gradient
limitation), feeding the Phase-B method evaluation.

DONE (jutari, ~20s):
  `julia --project=/Users/maier/Documents/code/UnderstandingVCS/jutari \
        tools/xai_study/ground_truth/oracle_xcheck.jl`
  test: `... tools/xai_study/ground_truth/test_oracle_xcheck.jl` → 19/19 pass.

Result (Pong, f120+30):
- CONTENT path — gradient-PREDICTED Δ vs EXACT finite Δ over a 16-point colour-step
  sweep × {COLUP0,COLUP1,COLUPF,COLUBK}: Spearman ρ = 1.0, Pearson r = 1.0,
  max|pred−exact| = 0.0 ⇒ the forward-exact one-hot content gradient (=1/unit) IS the
  exact oracle Δ on the content path. Companion validated.
- DISAGREEMENT (flagged, not hidden):
  * index (ball_x): naive ∂(pixel)/∂pos = 0 (round) vs intervention Δ = 20 (ball moves)
    ⇒ intervention oracle is the SOLE truth for position/index/event outputs.
  * non_smooth (sprite edge): naive 0 vs bilinear sampler 166 (documented workaround,
    not the oracle).
- VERDICT: the EXACT INTERVENTION ORACLE (P2-E1-1) is declared the reference instrument
  for Phases A/B/C; gradient is its companion on content outputs only, and gradient
  methods are evaluated as methods-under-test in Phase B (E4) for position/index/event.

Artifacts (SPEC §R): tools/xai_study/ground_truth/out/oracle_xcheck_pong.{json,npz}
(JSON parses + npz loads in numpy, verified). For E6 leaderboard / SM Review, the
record's metric_name = "spearman_content_path", value = 1.0.
