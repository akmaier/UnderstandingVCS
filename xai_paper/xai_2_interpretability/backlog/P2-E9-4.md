---
id: P2-E9-4
title: Final build + page/limit gate (SM)
epic: E9 (Reproducibility & submission prep)
status: done
sprint: 8
owner:
where: local
depends_on: [P2-E9-1, P2-E9-2, P2-E9-3]
file_scope:
  - paper/main.tex
  - paper/build/**
estimate: M
spec_ref: ../SPEC.md#e9-reproducibility-submission-prep-sprint-7-local-sm-led
---

## Goal
Run the final pre-submission build and gate: a clean `pdflatex` + `bibtex` cycle with no undefined
refs/citations or overfull-hbox blockers, the manuscript within all confirmed caps (length, abstract,
references, display items), and every required statement present. SM-led integration task that touches
the shared LaTeX master.

## Definition of Done
- [ ] clean build from this command: `cd paper && pdflatex main && bibtex main && pdflatex main && pdflatex main` (no undefined refs/citations, no overfull-hbox blockers)
- [ ] within the confirmed caps (length, abstract, references, display items — document_check.md §C/§G)
- [ ] all required statements present (document_check.md §D); reporting summary referenced
- [ ] nothing outside `file_scope` changed; Paper-1 gates untouched/green
- [ ] committed + pushed to main (rebase-before-push); primary pulled ff-only
- [ ] `status: done`

## Notes / handoff
SM-led per SPEC E9-4 ("SM"). SPEC scopes this as `paper/` broadly; restricted here to the master
`paper/main.tex` + a `paper/build/` output dir to stay disjoint from the per-section writing items.
This is the pre-submission gate (document_check.md §G); it runs after E9-1/-2/-3 are done.
NOTE: `paper/main.tex` is also in E8-0's scope — different sprints (6 vs 7), so the §0 per-sprint rule
holds, but the SM should be aware both touch the master.
