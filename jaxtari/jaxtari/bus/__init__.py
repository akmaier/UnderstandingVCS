"""6507 system bus.

See `jaxtari.bus.system` for the address decode + `peek` / `poke`.
"""

from jaxtari.bus.system import Bus, initial_bus, peek, poke

__all__ = ["Bus", "initial_bus", "peek", "poke"]
